<h1>Liste des produits</h1>

{% for message in app.flashes("info") %}
    <div>
        {{ message }}
    </div>
{% endfor %}

<ul>
    {% for product in products %}
        <li>{{ product.name }}</li>
    {% else %}
        Aucun produit
    {% endfor %}
</ul>